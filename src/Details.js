import React from "react";

const Details = (props) => {
  return (
    <div>
      <h1>Hey, that is details page</h1>
    </div>

    // <pre>
    //   <code>{ JSON.stringify(props, null, 4) }</code> {/* to debug props */}
    // </pre>
  );
};

export default Details;
